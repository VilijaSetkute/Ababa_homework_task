import React from 'react';
import Start from "../components/start/start";

const StartPage = () => {
    return (
        <div>
            <Start/>
        </div>
    );
};

export default StartPage;